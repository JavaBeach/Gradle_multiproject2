apply plugin: 'java'
apply plugin: 'eclipse'

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
    compile project(':MPF')
    compile project(':MP')
}

